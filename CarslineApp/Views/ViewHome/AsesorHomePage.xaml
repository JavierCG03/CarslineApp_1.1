<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CarslineApp.Views.AsesorHomePage"
             Shell.NavBarIsVisible="False"
             NavigationPage.HasNavigationBar="False"
             Title="Asesor de Servicio">

    <Grid RowDefinitions="Auto,Auto,Auto,*,Auto" Padding="0">

        <!-- ==================== HEADER ==================== -->
        <Frame Grid.Row="0" BackgroundColor="#D60000" Padding="20" Margin="0" CornerRadius="0">
            <VerticalStackLayout Spacing="5">
                <Label Text="ASESOR DE SERVICIO" FontSize="20" FontAttributes="Bold"
                       TextColor="White" HorizontalOptions="Center"/>
                <Label Text="{Binding NombreUsuarioActual}" FontSize="14"
                       TextColor="White" HorizontalOptions="Center"/>
            </VerticalStackLayout>
        </Frame>

        <!-- ==================== PESTAÑAS ==================== -->
        <Grid Grid.Row="1" ColumnDefinitions="*,*,*,*" BackgroundColor="#F5F5F5">
            <Button Grid.Column="0" Text="Servicio" Command="{Binding VerServicioCommand}"
                    BackgroundColor="{Binding EsServicio, Converter={StaticResource BoolToColorConverter}}"
                    TextColor="White" HeightRequest="50" FontSize="12" FontAttributes="Bold"/>

            <Button Grid.Column="1" Text="Diagnóstico" Command="{Binding VerDiagnosticoCommand}"
                    BackgroundColor="{Binding EsDiagnostico, Converter={StaticResource BoolToColorConverter}}"
                    TextColor="White" HeightRequest="50" FontSize="12" FontAttributes="Bold"/>

            <Button Grid.Column="2" Text="Reparación" Command="{Binding VerReparacionCommand}"
                    BackgroundColor="{Binding EsReparacion, Converter={StaticResource BoolToColorConverter}}"
                    TextColor="White" HeightRequest="50" FontSize="12" FontAttributes="Bold"/>

            <Button Grid.Column="3" Text="Garantía" Command="{Binding VerGarantiaCommand}"
                    BackgroundColor="{Binding EsGarantia, Converter={StaticResource BoolToColorConverter}}"
                    TextColor="White" HeightRequest="50" FontSize="12" FontAttributes="Bold"/>
        </Grid>

        <!-- ==================== TÍTULO + CREAR ORDEN ==================== -->
        <VerticalStackLayout Grid.Row="2" Padding="20" Spacing="10" BackgroundColor="White">
            
            <Label Text="{Binding TituloSeccion}"
                   FontSize="24" FontAttributes="Bold" TextColor="#D60000" HorizontalOptions="Center"/>

            <Button Text="+ CREAR NUEVA ORDEN"
                    Command="{Binding CrearOrdenCommand}"
                    BackgroundColor="#D60000" TextColor="White"
                    FontAttributes="Bold" HeightRequest="55" CornerRadius="10"/>

        </VerticalStackLayout>

        <!-- ==================== CONTENIDO SCROLL ==================== -->
        <RefreshView Grid.Row="3" IsRefreshing="{Binding IsLoading}" Command="{Binding RefreshCommand}">
            <ScrollView>
                <VerticalStackLayout Padding="20" Spacing="25">

                    <!-- ==================== ÓRDENES PENDIENTES ==================== -->
                    <Frame BackgroundColor="White" Padding="15" HasShadow="True" CornerRadius="10">
                        <VerticalStackLayout Spacing="10">
                            <Label Text="📋 ÓRDENES PENDIENTES"
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="#404040"/>

                            <BoxView HeightRequest="2" BackgroundColor="#E0E0E0"/>

                            <CollectionView ItemsSource="{Binding OrdenesPendientes}">
                                <CollectionView.EmptyView>
                                    <Label Text="✓ No hay órdenes pendientes"
                                           TextColor="#919191"
                                           FontSize="14"
                                           HorizontalOptions="Center"
                                           Margin="0,20"/>
                                </CollectionView.EmptyView>

                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Frame BackgroundColor="#FFF9F9"
                                               Padding="15"
                                               Margin="0,10,0,15"
                                               HasShadow="False"
                                               BorderColor="#D60000"
                                               CornerRadius="12">

                                            <VerticalStackLayout Spacing="12">

                                                <!-- ENCABEZADO: VEHÍCULO + NÚMERO ORDEN -->
                                                <Grid ColumnDefinitions="*,Auto">
                                                    <Label Grid.Column="0"
                                                           Text="{Binding VehiculoCompleto}"
                                                           FontSize="18"
                                                           FontAttributes="Bold"
                                                           TextColor="#1A1A1A"
                                                           LineBreakMode="TailTruncation"/>

                                                    <Frame Grid.Column="1"
                                                           BackgroundColor="#D60000"
                                                           Padding="8,4"
                                                           CornerRadius="6"
                                                           HasShadow="False">
                                                        <Label Text="{Binding NumeroOrden}"
                                                               FontSize="11"
                                                               FontAttributes="Bold"
                                                               TextColor="White"/>
                                                    </Frame>
                                                </Grid>

                                                <!-- INFO CLIENTE -->
                                                <Grid ColumnDefinitions="Auto,Auto,Auto,*" ColumnSpacing="8">
                                                    <Label Grid.Column="0" 
                                                           Text="👤" 
                                                           FontSize="14"/>
                                                    <Label Grid.Column="1"
                                                           Text="{Binding ClienteNombre}"
                                                           FontSize="14"
                                                           TextColor="#404040"
                                                           VerticalOptions="Center"/>
                                                    <Label Grid.Column="2" 
                                                           Text="📞" />
                                                    <Label Grid.Column="3"
                                                           Text="{Binding ClienteTelefono}"
                                                           FontSize="13"
                                                           TextColor="#666"
                                                           VerticalOptions="Center"/>
                                                </Grid>

                                                <!-- TIPO SERVICIO -->
                                                <Frame BackgroundColor="#FFF" 
                                                       Padding="8"
                                                       HasShadow="False"
                                                       BorderColor="#E0E0E0"
                                                       CornerRadius="6">
                                                    <Label Text="{Binding TipoServicio}"
                                                           FontSize="12"
                                                           TextColor="#666"
                                                           HorizontalOptions="Center"/>
                                                </Frame>

                                                <!-- HORA PROMESA -->
                                                <Grid ColumnDefinitions="Auto,*" ColumnSpacing="8">
                                                    <Label Grid.Column="0"
                                                           Text="⏰"
                                                           FontSize="14"/>
                                                    <Label Grid.Column="1"
                                                           Text="{Binding HoraPromesa}"
                                                           FontSize="14"
                                                           FontAttributes="Bold"
                                                           TextColor="#D60000"
                                                           VerticalOptions="Center"/>
                                                </Grid>

                                                <!-- PROGRESO DE TRABAJOS -->
                                                <Frame BackgroundColor="#F5F5F5"
                                                       Padding="12"
                                                       HasShadow="False"
                                                       CornerRadius="8"
                                                       IsVisible="{Binding TieneTrabajos}">
                                                    <VerticalStackLayout Spacing="8">

                                                        <!-- HEADER TRABAJOS -->
                                                        <Grid ColumnDefinitions="Auto,*,Auto">
                                                            <Label Grid.Column="0"
                                                                   Text="🔧"
                                                                   FontSize="14"
                                                                   VerticalOptions="Center"/>
                                                            <Label Grid.Column="1"
                                                                   FontSize="12"
                                                                   TextColor="#666"
                                                                   VerticalOptions="Center"
                                                                   Margin="5,0,0,0">
                                                                <Label.FormattedText>
                                                                    <FormattedString>
                                                                        <Span Text="Trabajos: "/>
                                                                        <Span Text="{Binding ProgresoTexto}" 
                                                                              FontAttributes="Bold"
                                                                              TextColor="#404040"/>
                                                                    </FormattedString>
                                                                </Label.FormattedText>
                                                            </Label>
                                                            <Label Grid.Column="2"
                                                                   Text="{Binding ProgresoFormateado}"
                                                                   FontSize="13"
                                                                   FontAttributes="Bold"
                                                                   TextColor="{Binding ColorProgreso}"
                                                                   VerticalOptions="Center"/>
                                                        </Grid>

                                                        <!-- BARRA PROGRESO CON CONTENEDOR -->
                                                        <Frame Padding="0" 
                                                               HasShadow="False" 
                                                               BackgroundColor="#E0E0E0" 
                                                               CornerRadius="8"
                                                               HeightRequest="12">
                                                            <ProgressBar Progress="{Binding ProgresoGeneral}"
                                                                         ProgressColor="{Binding ColorProgreso}"
                                                                         VerticalOptions="FillAndExpand"
                                                                         HorizontalOptions="FillAndExpand"/>
                                                        </Frame>

                                                        <!-- BOTÓN VER DETALLE -->
                                                        <Button Text="📋 Ver detalle de trabajos"
                                                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.VerDetalleOrdenCommand}"
                                                                CommandParameter="{Binding Id}"
                                                                BackgroundColor="#6C63FF"
                                                                TextColor="White"
                                                                FontSize="12"
                                                                HeightRequest="35"
                                                                CornerRadius="6"
                                                                Margin="0,5,0,0"/>
                                                    </VerticalStackLayout>
                                                </Frame>

                                                <!-- BOTÓN CANCELAR -->
                                                <Button Text="✖ CANCELAR ORDEN"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.CancelarOrdenCommand}"
                                                        CommandParameter="{Binding Id}"
                                                        BackgroundColor="#757575"
                                                        TextColor="White"
                                                        FontSize="13"
                                                        FontAttributes="Bold"
                                                        HeightRequest="45"
                                                        CornerRadius="8"/>
                                            </VerticalStackLayout>

                                        </Frame>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </VerticalStackLayout>
                    </Frame>

                    <!-- ==================== ÓRDENES EN PROCESO ==================== -->
                    <Frame BackgroundColor="White" Padding="15" HasShadow="True" CornerRadius="10">
                        <VerticalStackLayout Spacing="10">
                            <Label Text="⚙️ ÓRDENES EN PROCESO"
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="#404040"/>

                            <BoxView HeightRequest="2" BackgroundColor="#E0E0E0"/>

                            <CollectionView ItemsSource="{Binding OrdenesProceso}">
                                <CollectionView.EmptyView>
                                    <Label Text="✓ No hay órdenes en proceso"
                                           TextColor="#919191"
                                           FontSize="14"
                                           HorizontalOptions="Center"
                                           Margin="0,20"/>
                                </CollectionView.EmptyView>

                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Frame BackgroundColor="#FFF8E1"
                                               Padding="15"
                                               Margin="0,10,0,15"
                                               HasShadow="False"
                                               BorderColor="#FFA500"
                                               CornerRadius="12">

                                            <VerticalStackLayout Spacing="12">

                                                <!-- ENCABEZADO -->
                                                <Grid ColumnDefinitions="*,Auto">
                                                    <Label Grid.Column="0"
                                                           Text="{Binding VehiculoCompleto}"
                                                           FontSize="18"
                                                           FontAttributes="Bold"
                                                           TextColor="#1A1A1A"
                                                           LineBreakMode="TailTruncation"/>

                                                    <Frame Grid.Column="1"
                                                           BackgroundColor="#FFA500"
                                                           Padding="8,4"
                                                           CornerRadius="6"
                                                           HasShadow="False">
                                                        <Label Text="{Binding NumeroOrden}"
                                                               FontSize="11"
                                                               FontAttributes="Bold"
                                                               TextColor="White"/>
                                                    </Frame>
                                                </Grid>

                                                <!-- INFO CLIENTE -->
                                                <Grid ColumnDefinitions="Auto,Auto,Auto,*" ColumnSpacing="8">
                                                    <Label Grid.Column="0" 
                                                           Text="👤" 
                                                           FontSize="14"/>
                                                    <Label Grid.Column="1"
                                                           Text="{Binding ClienteNombre}"
                                                           FontSize="14"
                                                           TextColor="#404040"
                                                           VerticalOptions="Center"/>
                                                    <Label Grid.Column="2" 
                                                           Text="📞" />
                                                    <Label Grid.Column="3"
                                                           Text="{Binding ClienteTelefono}"
                                                           FontSize="13"
                                                           TextColor="#666"
                                                           VerticalOptions="Center"/>
                                                </Grid>

                                                <!-- TÉCNICO ASIGNADO -->
                                                <Frame BackgroundColor="#FFF3E0"
                                                       Padding="10"
                                                       HasShadow="False"
                                                       BorderColor="#FFA500"
                                                       CornerRadius="8">
                                                    <Grid ColumnDefinitions="Auto,*">
                                                        <Label Grid.Column="0"
                                                               Text="👨‍🔧"
                                                               FontSize="16"
                                                               VerticalOptions="Center"/>
                                                        <Label Grid.Column="1"
                                                               Text="{Binding NombreTecnico}"
                                                               FontSize="14"
                                                               FontAttributes="Bold"
                                                               TextColor="#FF8C00"
                                                               VerticalOptions="Center"
                                                               Margin="8,0,0,0"/>
                                                    </Grid>
                                                </Frame>

                                                <!-- HORA PROMESA -->
                                                <Grid ColumnDefinitions="Auto,*" ColumnSpacing="8">
                                                    <Label Grid.Column="0" Text="⏰" FontSize="14"/>
                                                    <Label Grid.Column="1"
                                                           Text="{Binding HoraPromesa}"
                                                           FontSize="14"
                                                           FontAttributes="Bold"
                                                           TextColor="#D60000"
                                                           VerticalOptions="Center"/>
                                                </Grid>

                                                <!-- PROGRESO DE TRABAJOS -->
                                                <Frame BackgroundColor="#FFFBF5"
                                                       Padding="12"
                                                       HasShadow="False"
                                                       CornerRadius="8"
                                                       IsVisible="{Binding TieneTrabajos}">
                                                    <VerticalStackLayout Spacing="8">

                                                        <Grid ColumnDefinitions="Auto,*,Auto">
                                                            <Label Grid.Column="0"
                                                                   Text="🔧"
                                                                   FontSize="14"
                                                                   VerticalOptions="Center"/>
                                                            <Label Grid.Column="1"
                                                                   FontSize="12"
                                                                   TextColor="#666"
                                                                   VerticalOptions="Center"
                                                                   Margin="5,0,0,0">
                                                                <Label.FormattedText>
                                                                    <FormattedString>
                                                                        <Span Text="Trabajos: "/>
                                                                        <Span Text="{Binding ProgresoTexto}" 
                                                                              FontAttributes="Bold"
                                                                              TextColor="#404040"/>
                                                                    </FormattedString>
                                                                </Label.FormattedText>
                                                            </Label>
                                                            <Label Grid.Column="2"
                                                                   Text="{Binding ProgresoFormateado}"
                                                                   FontSize="13"
                                                                   FontAttributes="Bold"
                                                                   TextColor="{Binding ColorProgreso}"
                                                                   VerticalOptions="Center"/>
                                                        </Grid>

                                                        <Frame Padding="0" 
                                                               HasShadow="False" 
                                                               BackgroundColor="#E0E0E0" 
                                                               CornerRadius="8"
                                                               HeightRequest="12">
                                                            <ProgressBar Progress="{Binding ProgresoGeneral}"
                                                                         ProgressColor="{Binding ColorProgreso}"
                                                                         VerticalOptions="FillAndExpand"
                                                                         HorizontalOptions="FillAndExpand"/>
                                                        </Frame>

                                                        <Button Text="📋 Ver detalle de trabajos"
                                                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.VerDetalleOrdenCommand}"
                                                                CommandParameter="{Binding Id}"
                                                                BackgroundColor="#6C63FF"
                                                                TextColor="White"
                                                                FontSize="12"
                                                                HeightRequest="35"
                                                                CornerRadius="6"
                                                                Margin="0,5,0,0"/>
                                                    </VerticalStackLayout>
                                                </Frame>
                                            </VerticalStackLayout>

                                        </Frame>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </VerticalStackLayout>
                    </Frame>

                    <!-- ==================== ÓRDENES FINALIZADAS ==================== -->
                    <Frame BackgroundColor="White" Padding="15" HasShadow="True" CornerRadius="10">
                        <VerticalStackLayout Spacing="10">
                            <Label Text="✅ ÓRDENES FINALIZADAS"
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="#404040"/>

                            <BoxView HeightRequest="2" BackgroundColor="#E0E0E0"/>

                            <CollectionView ItemsSource="{Binding OrdenesFinalizadas}">
                                <CollectionView.EmptyView>
                                    <Label Text="✓ No hay órdenes finalizadas"
                                           TextColor="#919191"
                                           FontSize="14"
                                           HorizontalOptions="Center"
                                           Margin="0,20"/>
                                </CollectionView.EmptyView>

                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Frame BackgroundColor="#F1F8F4"
                                               Padding="15"
                                               Margin="0,10,0,15"
                                               HasShadow="False"
                                               BorderColor="#4CAF50"
                                               CornerRadius="12">

                                            <VerticalStackLayout Spacing="12">

                                                <!-- ENCABEZADO -->
                                                <Grid ColumnDefinitions="*,Auto">
                                                    <Label Grid.Column="0"
                                                           Text="{Binding VehiculoCompleto}"
                                                           FontSize="18"
                                                           FontAttributes="Bold"
                                                           TextColor="#1A1A1A"
                                                           LineBreakMode="TailTruncation"/>

                                                    <Frame Grid.Column="1"
                                                           BackgroundColor="#4CAF50"
                                                           Padding="8,4"
                                                           CornerRadius="6"
                                                           HasShadow="False">
                                                        <Label Text="{Binding NumeroOrden}"
                                                               FontSize="11"
                                                               FontAttributes="Bold"
                                                               TextColor="White"/>
                                                    </Frame>
                                                </Grid>

                                                <!-- INFO CLIENTE -->
                                                <Grid ColumnDefinitions="Auto,Auto,Auto,*" ColumnSpacing="8">
                                                    <Label Grid.Column="0" 
                                                           Text="👤" 
                                                           FontSize="14"/>
                                                    <Label Grid.Column="1"
                                                           Text="{Binding ClienteNombre}"
                                                           FontSize="14"
                                                           TextColor="#404040"
                                                           VerticalOptions="Center"/>
                                                    <Label Grid.Column="2" 
                                                           Text="📞" />
                                                    <Label Grid.Column="3"
                                                           Text="{Binding ClienteTelefono}"
                                                           FontSize="13"
                                                           TextColor="#666"
                                                           VerticalOptions="Center"/>
                                                </Grid>

                                                <!-- TÉCNICO -->
                                                <Frame BackgroundColor="#E8F5E9"
                                                       Padding="10"
                                                       HasShadow="False"
                                                       BorderColor="#4CAF50"
                                                       CornerRadius="8">
                                                    <Grid ColumnDefinitions="Auto,*">
                                                        <Label Grid.Column="0"
                                                               Text="👨‍🔧"
                                                               FontSize="16"
                                                               VerticalOptions="Center"/>
                                                        <Label Grid.Column="1"
                                                               Text="{Binding NombreTecnico}"
                                                               FontSize="14"
                                                               FontAttributes="Bold"
                                                               TextColor="#2E7D32"
                                                               VerticalOptions="Center"
                                                               Margin="8,0,0,0"/>
                                                    </Grid>
                                                </Frame>

                                                <!-- HORA FIN -->
                                                <Grid ColumnDefinitions="Auto,*" ColumnSpacing="8">
                                                    <Label Grid.Column="0" Text="✓" FontSize="14" TextColor="#4CAF50"/>
                                                    <Label Grid.Column="1"
                                                           Text="{Binding HoraFin}"
                                                           FontSize="14"
                                                           FontAttributes="Bold"
                                                           TextColor="#4CAF50"
                                                           VerticalOptions="Center"/>
                                                </Grid>

                                                <!-- PROGRESO COMPLETADO -->
                                                <Frame BackgroundColor="#F9FFF9"
                                                       Padding="12"
                                                       HasShadow="False"
                                                       CornerRadius="8"
                                                       IsVisible="{Binding TieneTrabajos}">
                                                    <VerticalStackLayout Spacing="8">

                                                        <Grid ColumnDefinitions="Auto,*,Auto">
                                                            <Label Grid.Column="0"
                                                                   Text="✅"
                                                                   FontSize="14"
                                                                   VerticalOptions="Center"/>
                                                            <Label Grid.Column="1"
                                                                   FontSize="12"
                                                                   TextColor="#666"
                                                                   VerticalOptions="Center"
                                                                   Margin="5,0,0,0">
                                                                <Label.FormattedText>
                                                                    <FormattedString>
                                                                        <Span Text="Trabajos: "/>
                                                                        <Span Text="{Binding ProgresoTexto}" 
                                                                              FontAttributes="Bold"
                                                                              TextColor="#404040"/>
                                                                    </FormattedString>
                                                                </Label.FormattedText>
                                                            </Label>
                                                            <Label Grid.Column="2"
                                                                   Text="{Binding ProgresoFormateado}"
                                                                   FontSize="13"
                                                                   FontAttributes="Bold"
                                                                   TextColor="#4CAF50"
                                                                   VerticalOptions="Center"/>
                                                        </Grid>

                                                        <Frame Padding="0" 
                                                               HasShadow="False" 
                                                               BackgroundColor="#E0E0E0" 
                                                               CornerRadius="8"
                                                               HeightRequest="12">
                                                            <ProgressBar Progress="{Binding ProgresoGeneral}"
                                                                         ProgressColor="#4CAF50"
                                                                         VerticalOptions="FillAndExpand"
                                                                         HorizontalOptions="FillAndExpand"/>
                                                        </Frame>

                                                        <Button Text="📋 Ver detalle de trabajos"
                                                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.VerDetalleOrdenCommand}"
                                                                CommandParameter="{Binding Id}"
                                                                BackgroundColor="#6C63FF"
                                                                TextColor="White"
                                                                FontSize="12"
                                                                HeightRequest="35"
                                                                CornerRadius="6"
                                                                Margin="0,5,0,0"/>
                                                    </VerticalStackLayout>
                                                </Frame>

                                                <!-- BOTÓN ENTREGAR -->
                                                <Button Text="🚗 ENTREGAR VEHÍCULO"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EntregarOrdenCommand}"
                                                        CommandParameter="{Binding Id}"
                                                        BackgroundColor="#4CAF50"
                                                        TextColor="White"
                                                        FontSize="13"
                                                        FontAttributes="Bold"
                                                        HeightRequest="45"
                                                        CornerRadius="8"/>
                                            </VerticalStackLayout>

                                        </Frame>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </VerticalStackLayout>
                    </Frame>

                </VerticalStackLayout>
            </ScrollView>
        </RefreshView>

        <!-- ==================== CERRAR SESIÓN ==================== -->
        <Button Grid.Row="4"
                Text="🚪 CERRAR SESIÓN"
                Command="{Binding LogoutCommand}"
                BackgroundColor="#404040"
                TextColor="White"
                FontSize="14"
                FontAttributes="Bold"
                HeightRequest="55"/>
    </Grid>

</ContentPage>