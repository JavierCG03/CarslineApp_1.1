<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CarslineApp.Views.CheckListServicioPage"
             Title="Checklist de Servicio">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="16">

            <Label Text="Checklist de Servicio"
                   FontSize="22"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"/>

            <!-- EJEMPLOS -->
            <CheckBox />
            <Label Text="Nivel de aceite"/>

            <CheckBox />
            <Label Text="Filtro de aceite"/>

            <CheckBox />
            <Label Text="Presión de llantas"/>

            <Button Text="Guardar Checklist"
                    BackgroundColor="#4CAF50"
                    TextColor="White"
                    CornerRadius="10"
                    HeightRequest="50"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>

